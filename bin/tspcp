#!/bin/bash
# Copy all finished tsp logs to folder

if [ -z "$1" ]; then echo "No directory supplied"; exit 1; fi

mkdir -p $1

# Copy individual log files
tsp | grep -w finished | tr -s ' ' | cut -d' ' -f 3 | xargs cp -t "$1/"

# Copy log
tsp > "$1/tsp.log"
